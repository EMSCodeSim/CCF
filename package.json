{
  "scripts": {
    "build": "echo \"No build step (static web in CCF/)\"",
    "cap:copy": "npx cap copy android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  }
}
