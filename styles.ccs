:root{
  --bg:#0e1116;
  --card:#161b22;
  --card2:#1b2230;
  --text:#e6edf3;
  --muted:#9aa4b2;
  --green:#22c55e;
  --red:#ef4444;
  --line:#2a3342;
  --shadow: 0 10px 30px rgba(0,0,0,.35);
  --radius:18px;
}

*{box-sizing:border-box}
body{
  margin:0;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background:var(--bg);
  color:var(--text);
}

.appbar{
  position:sticky; top:0;
  display:flex; align-items:center; gap:10px;
  padding:12px 14px;
  background:#0b0e13;
  border-bottom:1px solid #111826;
  z-index:10;
}
.title{font-weight:700; font-size:18px; flex:1; text-align:center;}
.actions{display:flex; gap:6px}
.iconbtn{
  width:40px; height:40px;
  border:none; border-radius:12px;
  background:var(--card);
  color:var(--text);
  font-size:18px;
  cursor:pointer;
}
.iconbtn:active{transform:scale(.98)}

.container{max-width:560px; margin:0 auto; padding:14px;}

.statusRow{
  display:flex; gap:12px;
  background:var(--card);
  border:1px solid #0f1522;
  border-radius:var(--radius);
  padding:12px 14px;
  box-shadow: var(--shadow);
}
.label{color:var(--muted); font-size:12px; letter-spacing:.05em; text-transform:uppercase;}
.value{font-size:16px; font-weight:700; margin-top:4px;}
.status{flex:1}
.ccf{text-align:right; min-width:120px}

.timerBlock{
  margin-top:14px;
  background:linear-gradient(180deg, var(--card), #121826);
  border:1px solid #0f1522;
  border-radius:var(--radius);
  padding:20px 14px;
  text-align:center;
  box-shadow: var(--shadow);
}
.bigTime{
  font-size:64px;
  font-weight:800;
  line-height:1;
}
.cyclePill{
  display:inline-block;
  margin-top:10px;
  padding:8px 12px;
  border-radius:999px;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.08);
  color:var(--muted);
  font-weight:600;
}

.actionsRow{
  margin-top:14px;
  display:flex;
  gap:12px;
}
.btn{
  flex:1;
  border:none;
  border-radius:20px;
  padding:18px 14px;
  font-size:18px;
  font-weight:800;
  cursor:pointer;
  box-shadow: var(--shadow);
}
.btnGreen{background:linear-gradient(180deg, #22c55e, #16a34a); color:#05210f;}
.btnRed{background:linear-gradient(180deg, #ef4444, #b91c1c); color:#2a0707;}
.btn:active{transform:scale(.99)}

.cardsRow{
  margin-top:14px;
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:10px;
}
.card{
  background:var(--card);
  border:1px solid #0f1522;
  border-radius:16px;
  padding:12px 12px;
  box-shadow: var(--shadow);
}
.cardLabel{color:var(--muted); font-size:12px}
.cardValue{font-size:20px; font-weight:800; margin-top:6px}

.timelineSection{
  margin-top:14px;
  background:var(--card);
  border:1px solid #0f1522;
  border-radius:var(--radius);
  padding:12px;
  box-shadow: var(--shadow);
}
.timelineHeader{display:flex; justify-content:space-between; align-items:center; margin-bottom:8px;}
.linkbtn{
  border:none; background:transparent; color:#7dd3fc;
  font-weight:700; cursor:pointer;
}
.timelineStrip{
  height:18px;
  border-radius:999px;
  overflow:hidden;
  display:flex;
  border:1px solid rgba(255,255,255,.08);
  background:rgba(255,255,255,.04);
}
.seg{height:100%}
.seg.green{background:rgba(34,197,94,.95)}
.seg.red{background:rgba(239,68,68,.95)}
.hint{color:var(--muted); font-size:12px; margin-top:8px}

.overlay{
  position:fixed; inset:0;
  background:rgba(0,0,0,.5);
  z-index:50;
}
.sheet{
  position:fixed; left:0; right:0; bottom:0;
  background:#0b0f16;
  border-top-left-radius:22px;
  border-top-right-radius:22px;
  border:1px solid #121a2a;
  z-index:60;
  padding:14px;
  box-shadow: 0 -20px 40px rgba(0,0,0,.5);
}
.hidden{display:none}

.sheetHeader{
  display:flex; justify-content:space-between; align-items:center;
}
.sheetTitle{font-size:16px; font-weight:900}
.sheetSub{font-size:12px; color:var(--muted); margin-top:2px}

.chipGrid{
  margin-top:12px;
  display:grid;
  grid-template-columns: repeat(2, 1fr);
  gap:10px;
}
.chip{
  border:1px solid #1a2438;
  background:var(--card2);
  color:var(--text);
  border-radius:16px;
  padding:14px 12px;
  font-weight:800;
  cursor:pointer;
  text-align:center;
}
.chip:active{transform:scale(.99)}

.sheetFooter{
  margin-top:12px;
  display:flex; justify-content:space-between; align-items:center;
  gap:12px;
  color:var(--muted);
}
.toggle{display:flex; gap:8px; align-items:center; font-weight:700;}
.warnRow{display:flex; gap:8px; align-items:center;}
select{
  background:var(--card);
  border:1px solid #1a2438;
  color:var(--text);
  border-radius:12px;
  padding:8px 10px;
}
.small{font-size:12px}
